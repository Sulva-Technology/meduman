# --- Frontend (exposed to the browser via VITE_ prefix) ---
VITE_SITE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_URL=
# Leave blank in dev to use the /api proxy. Set to the deployed NestJS origin in prod.
VITE_API_BASE_URL=

# --- Dev server only (not exposed to the browser) ---
# Where the Vite /api proxy forwards to. Must match the NestJS PORT.
API_PROXY_TARGET=http://localhost:3000

# --- Local Express (waitlist email) ---
RESEND_API_KEY=
SMTP_FROM=
SMTP_HOST=
SMTP_PASS=
SMTP_PORT=
SMTP_USER=
GEMINI_API_KEY=
